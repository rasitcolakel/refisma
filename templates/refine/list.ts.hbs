{{> imports name=name imports=imports }}

export default function {{name}}List() {
    const t = useTranslate();
    const { dataGridProps } = useDataGrid<{{name}}>();

    {{> columns fields=excludedModels name=name actions=actions idField=idField }}

    return (
      <List canCreate>
        <DataGrid
          {...dataGridProps}
          autoHeight
          columns={columns}
          density="comfortable"
        />
      </List>
    );
}

{{> getServerSideProps }}

