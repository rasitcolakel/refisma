const columns = React.useMemo<GridColumns<{{name}}>>(
  () => [
    {{#each fields}}
    
    {
    field: '{{this.name}}',
    headerName: t('table.{{this.name}}'),
    } {{#unless @last}},{{/unless}}
    {{/each}}
  ],
  [t],
);